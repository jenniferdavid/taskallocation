ROS action command-line tool
============================

This is a ROS action command-line tool that works just like the built-in
`rostopic` and `rosservice` commands.

For now, all functionality is there except for autocompletion.
